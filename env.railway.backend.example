# Railway Environment Variables - Backend Service
# Skopiuj te zmienne do Railway Dashboard → Backend Service → Settings → Variables

# ============================================================================
# WYMAGANE ZMIENNE
# ============================================================================

PORT=8000
HOST=0.0.0.0
USE_REALTIME_API=true

# ============================================================================
# OPCJONALNE - Railway Service Discovery
# ============================================================================
# Tylko jeśli używasz osobnych serwisów dla baz danych w Railway
# Zastąp nazwy serwisów rzeczywistymi nazwami z Railway Dashboard

# Pocketbase
POCKETBASE_URL=http://pocketbase.railway.internal:8090

# QuestDB
QUESTDB_HOST=questdb.railway.internal
QUESTDB_PORT=9009
QUESTDB_USER=admin
QUESTDB_PASSWORD=quest

# Redis
REDIS_URL=redis://redis.railway.internal:6379

# ============================================================================
# ALTERNATYWA - Publiczne URL-e
# ============================================================================
# Jeśli twoje bazy danych mają publiczne URL-e:

# POCKETBASE_URL=https://pocketbase-production-XXXX.up.railway.app
# QUESTDB_HOST=questdb-production-XXXX.up.railway.app
# REDIS_URL=redis://redis-production-XXXX.up.railway.app:6379

# ============================================================================
# INSTRUKCJE
# ============================================================================
#
# 1. W Railway Dashboard → Backend Service → Settings → Variables
# 2. Dodaj WYMAGANE zmienne (PORT, HOST)
# 3. Jeśli masz osobne serwisy dla baz danych:
#    - Zastąp nazwy serwisów rzeczywistymi (pocketbase, questdb, redis)
#    - Format internal: <service-name>.railway.internal:<port>
# 4. Kod automatycznie użyje tych zmiennych (fallback: localhost dla dev)
#

